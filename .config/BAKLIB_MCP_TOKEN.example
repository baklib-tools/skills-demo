# 使用说明：
# 1. 复制本文件为同目录下的 BAKLIB_MCP_TOKEN（无 .example 后缀）
# 2. 删除本文件内所有以 # 开头的行
# 3. 只保留一行，格式：<key>:<secret>（Baklib 后台 → 个人中心 → API 密钥对）
#
# 示例（请换成你自己的真实密钥，不要带引号、不要 KEY= 前缀）：
# myappkey:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
